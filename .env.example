# Copy to .env and fill in. OPENAI_API_KEY is required for live-keyed runs (AC20)
# and for the scripted-interface parity live ACs. TAVILY_API_KEY is only used if D15
# is switched to web search (not the default).
OPENAI_API_KEY=
TAVILY_API_KEY=
